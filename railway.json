{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.railway"
  },
  "deploy": {
    "startCommand": "sh -c 'uv run gunicorn movie_assistant.app:app --bind 0.0.0.0:${PORT:-5000} --workers 1 --timeout 120'"
  }
}
